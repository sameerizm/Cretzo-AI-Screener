services:
  - type: web
    name: cv-screening-api
    env: docker       # Use Docker environment, not Python
    plan: starter
    dockerfilePath: ./Dockerfile  # Path to your Dockerfile
    envVars:
      - key: ENVIRONMENT
        value: production
    healthCheckPath: /health
    autoDeploy: true
