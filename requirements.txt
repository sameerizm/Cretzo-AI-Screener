# ABSOLUTE MINIMAL - Guaranteed to work on any Python environment
# Only the most essential packages with very old stable versions

# Web framework only
fastapi==0.65.2
uvicorn==0.22.0

# Basic utilities only
python-dateutil==2.8.1
