# Core web framework (guaranteed to work)
fastapi==0.68.0
uvicorn==0.15.0

# AI libraries (optional - will gracefully fallback if not available)
sentence-transformers==2.2.2
torch==1.13.1
numpy==1.21.6

# Document processing (optional - will fallback if not available)
pdfplumber==0.7.4
python-docx==0.8.11

# PDF generation (optional)
fpdf==2.5.4
