services:
  - type: web
    name: cv-screening-api
    env: docker
    plan: starter
    dockerfilePath: ./Dockerfile   # points to your Dockerfile
    envVars:
      - key: ENVIRONMENT
        value: production
    healthCheckPath: /health

